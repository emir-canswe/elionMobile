package com.elion.assistant.ui.theme

import androidx.compose.ui.graphics.Color

// ───────── Ana renkler ─────────
val Primary         = Color(0xFF0D0D0D)   // Koyu arka plan
val Surface         = Color(0xFF161616)   // Kart arka planı
val SurfaceVariant  = Color(0xFF1C1C1C)   // İkincil kart
val Accent          = Color(0xFF6C63FF)   // Vurgu (mor)
val AccentLight     = Color(0xFF9B8FFF)   // Açık vurgu

// ───────── Öncelik renkleri ─────────
val PriorityHigh    = Color(0xFFE94560)   // Kırmızı — acil
val PriorityNormal  = Color(0xFFF5A623)   // Sarı — normal
val PriorityLow     = Color(0xFF4ECDC4)   // Teal — bekleyebilir

// ───────── Durum renkleri ─────────
val Success         = Color(0xFF2ECC71)
val Warning         = Color(0xFFF39C12)
val Danger          = Color(0xFFE74C3C)

// ───────── Metin renkleri ─────────
val TextPrimary     = Color(0xFFEEEEEE)
val TextSecondary   = Color(0xFF9E9E9E)
val TextTertiary    = Color(0xFF555555)

// ───────── Border / Divider ─────────
val BorderColor     = Color(0xFF2A2A2A)
val DividerColor    = Color(0xFF1E1E1E)

// ───────── Kategori renkleri ─────────
val CategoryWork    = Color(0xFF4A90D9)
val CategoryPersonal = Color(0xFF7B68EE)
val CategoryHealth  = Color(0xFF2ECC71)
val CategoryShopping = Color(0xFFF39C12)
val CategoryFinance = Color(0xFFE74C3C)
val CategoryOther   = Color(0xFF9E9E9E)

// ───────── Kategori arka planları (yarı saydam) ─────────
val CategoryWorkBg      = Color(0xFF1A2744)
val CategoryPersonalBg  = Color(0xFF2A1A44)
val CategoryHealthBg    = Color(0xFF1A2F1A)
val CategoryShoppingBg  = Color(0xFF2F2A1A)
val CategoryFinanceBg   = Color(0xFF2F1A1A)
val CategoryOtherBg     = Color(0xFF222222)
